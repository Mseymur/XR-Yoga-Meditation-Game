using UnityEngine;

public static class EnvironmentData
{
    // A 'static' variable belongs to the class itself, not an object in the scene.
    // This means its value will persist after a scene load.
    public static Material selectedSkyboxMaterial;
}